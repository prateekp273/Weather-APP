name: weather_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  http: ^0.13.4
  weather_icons: ^3.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.17.0 # Added for date and time formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

